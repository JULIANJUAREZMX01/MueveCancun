# CancúnMueve: Perpendicular Agent Protocol

## 🤖 Agent Roles & Responsibilities

### Jules (The Builder / Core)
- **Scope**: `rust-wasm/`, `public/sw.js`, `package.json` (build scripts), `src/wasm/`.
- **Primary Task**: Architectural integrity, Rust/WASM high-performance algorithms, Service Worker caching, and CI/CD pipelines.
- **Communication**: Responds to PR reviews and issues on GitHub.

### Gemini 3 Flash (The Observer / Researcher)
- **Scope**: `src/pages/`, `src/layouts/`, `src/components/`, `src/data/research/`, `AGENTS.md`.
- **Primary Task**: UI/UX refinement (Astro/Tailwind), "Sunny Mode" optimization, real-world data research (MCP Search), and user experience auditing.
- **Communication**: Orchestrates parallel work and manages the `task.md`, `implementation_plan.md`, and `AGENTS.md`.

## 🏗️ Technical Constraints
- **Offline-First**: All routing MUST happen locally via the WASM module. No API calls for core navigation.
- **Sunny Mode**: High-contrast colors only (Caribbean Blue, Sand, Sun Yellow). Deep Navy for text.
- **WASM Bridge**: Maintain `@/wasm/route_calculator/` as the single source for routing logic.
- **Strict Typing**: No `any`. Everything must have an interface or type definition.

## 📡 MCP Strategy
- **Google Search**: Use `search_web` for real-time traffic updates and Cancun fare changes.
- **Sequential Thinking**: Plan migrations (like Astro 5.0) step-by-step.
- **Supabase**: Only for crowd-sourced metadata, not for core functionality.
