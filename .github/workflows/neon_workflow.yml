name: Neon Branch Preview — Create/Delete on PR

# Crea una rama Neon aislada para cada PR
# La rama tiene los mismos datos que producción pero está aislada
# Se elimina automáticamente cuando el PR se cierra

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - closed

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}

jobs:
  setup:
    name: "Setup — obtener nombre de branch"
    outputs:
      branch: ${{ steps.branch_name.outputs.current_branch }}
    runs-on: ubuntu-latest
    steps:
      - name: Get branch name
        id: branch_name
        uses: tj-actions/branch-names@6871f53176ad61624f978536bbf089c574dc19a2 # v8.0.1

  create_neon_branch:
    name: "Crear branch Neon para PR #${{ github.event.number }}"
    outputs:
      db_url: ${{ steps.create_neon_branch.outputs.db_url }}
    needs: setup
    if: |
      github.event_name == 'pull_request' && (
        github.event.action == 'opened' ||
        github.event.action == 'reopened' ||
        github.event.action == 'synchronize'
      )
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4

      - name: Calcular fecha de expiración (2 semanas)
        run: echo "EXPIRES_AT=$(date -u --date '+14 days' +'%Y-%m-%dT%H:%M:%SZ')" >> "$GITHUB_ENV"
      - name: Create Neon Branch
        id: create_neon_branch
        uses: neondatabase/create-branch-action@5c59720f0092955bc8b846ac0cd184b1093c27a2 # v6.0.0
        with:
          project_id: mute-waterfall-29421911
          branch_name: preview/pr-${{ github.event.number }}-neon
          api_key: ${{ secrets.NEON_API_KEY }}

      - name: Setup Node 24
        uses: actions/setup-node@1d0ff469b7ec7b3cb9d8673fde0c81c44821de2a # v4
        with:
          node-version: "24"

      - name: Setup pnpm
        uses: pnpm/action-setup@7088e561eb65bb68695d245aa206f005ef30921d # v4

      - name: Instalar dependencias
        run: pnpm install --frozen-lockfile

      - name: Aplicar schema a branch Neon del PR
        run: |
          node --input-type=module << 'NODE_EOF'
          import { neon } from "@neondatabase/serverless";
          const dsn = process.env.DATABASE_URL;
          if (!dsn) {
            console.error("❌ Error: DATABASE_URL is empty or undefined.");
            process.exit(1);
          }
          const sql = neon(dsn);
          try {
            await sql`
              CREATE TABLE IF NOT EXISTS guardians (
                email               TEXT,
                stripe_customer_id  TEXT PRIMARY KEY,
                tier                TEXT,
                amount_monthly      NUMERIC,
                status              TEXT NOT NULL DEFAULT 'active',
                created_at          TIMESTAMPTZ DEFAULT NOW(),
                updated_at          TIMESTAMPTZ DEFAULT NOW()
              )
            `;
            await sql`
              CREATE TABLE IF NOT EXISTS payments (
                id                  TEXT PRIMARY KEY DEFAULT gen_random_uuid()::text,
                stripe_customer_id  TEXT NOT NULL,
                amount              NUMERIC,
                status              TEXT NOT NULL DEFAULT 'pending',
                stripe_payment_id   TEXT,
                created_at          TIMESTAMPTZ DEFAULT NOW()
              )
            `;
            console.log("✅ Schema aplicado al branch Neon del PR");
          } catch (e) {
            console.error("❌ Fallo al aplicar schema:", e);
            process.exit(1);
          }
          NODE_EOF
        env:
          DATABASE_URL: ${{ steps.create_neon_branch.outputs.db_url }}

      - name: Comentar en PR con URL del branch Neon
        uses: actions/github-script@60a0d83039c74a4aee543508d2ffcb1c3799cdea # v7
        with:
          script: |
            const branch = "preview/pr-${{ github.event.number }}-neon";
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: ${{ github.event.number }},
              body: [
                "## 🐘 Neon Branch creada para este PR",
                "",
                `**Branch:** \`${branch}\``,
                "**Expira en:** 14 días",
                "",
                "El schema de Guardians + Payments fue aplicado automáticamente.",
                "Vercel Preview usará esta DB aislada.",
                "",
                "> Generado automáticamente por neon_workflow.yml",
              ].join("\n"),
            });

  delete_neon_branch:
    name: "Eliminar branch Neon — PR #${{ github.event.number }} cerrado"
    needs: setup
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    steps:
      - name: Delete Neon Branch
        uses: neondatabase/delete-branch-action@cc0fe5eeb96c99cd4df545047d5dfdff84c5bddb # v3.0.0
        with:
          project_id: mute-waterfall-29421911
          branch_name: preview/pr-${{ github.event.number }}-neon
          api_key: ${{ secrets.NEON_API_KEY }}
