<<<<<<< SEARCH
      function enforceTutorial() {
        const isTutorialPage = window.location.pathname === "/" || window.location.pathname === "";
        const isStaticAsset = window.location.pathname.includes(".") || window.location.pathname.startsWith("/_astro") || window.location.pathname.startsWith("/data/");

        const isCompleted = document.cookie.includes("tutorial_completed=true") || localStorage.getItem("tutorial_completed") === "true";

        if (!isTutorialPage && !isStaticAsset && !isCompleted) {
          window.location.href = "/";
        }
      }
=======
      function enforceTutorial() {
        const isTutorialPage = window.location.pathname === "/" || window.location.pathname === "";
        const isStaticAsset = window.location.pathname.includes(".") || window.location.pathname.startsWith("/_astro") || window.location.pathname.startsWith("/data/");

        const isCompleted = document.cookie.includes("tutorial_completed=true") || localStorage.getItem("tutorial_completed") === "true";
        console.log("[enforceTutorial]", { path: window.location.pathname, isCompleted, isTutorialPage });

        if (!isTutorialPage && !isStaticAsset && !isCompleted) {
          console.log("[enforceTutorial] Redirecting to /");
          window.location.href = "/";
        }
      }
>>>>>>> REPLACE
