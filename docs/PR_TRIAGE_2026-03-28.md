# Auditoría y Triage de PRs — 2026-03-28

> **Ejecutado por**: GitHub Copilot Agent (claude-code)
> **Fecha**: 2026-03-28T22:10 UTC
> **Total PRs auditadas**: 20 (open)
> **Estado de main en el momento del análisis**: commit `9a9835017c21c44f8ab0631c609fcd421b26fa88`

---

## Resumen Ejecutivo

Se auditaron 20 PRs abiertas. Se clasificaron en 4 grupos según su CI, conflictos con `main` y calidad
del código. El resultado define cuáles mergear inmediatamente, cuáles requieren rebase previo, cuáles
bloquear y cuáles ignorar (drafts).

---

## GRUPO 1 — ✅ Limpias y mergeables inmediatamente

Todas tienen `mergeable_state: clean` y la suite completa de CI en verde.

| PR | Título | Archivos | +/- | Prioridad |
|----|--------|----------|-----|-----------|
| **#366** | bump brace-expansion 1.1.13 + picomatch 2.3.2 | 1 | +26/-26 | 🔴 URGENTE (CVE) |
| **#350** | Fix Event Delegation for Map Buttons | 3 | +16/-24 | Alta |
| **#351** | ⚡ Optimize Set iteration CoordinateFinder | 3 | +14/-7 | Media |
| **#352** | 🧹 Error handling + telemetry FavoritesStore | 5 | +62/-7 | Media |

### Detalle de seguridad — #366
- `picomatch 2.3.2` corrige **CVE-2026-33671** y **CVE-2026-33672**
- `brace-expansion 1.1.13` corrige **GHSA-f886-m6hf-6m8v**
- Son dependencias indirectas del toolchain de build (Vite/Vitest). Mergear sin demora.

### Detalle técnico — #350
- Agrega `e.isTrusted` guard en handlers de botones del mapa.
- Implementa singleton `window.__RouteCalculatorDelegationAttached` para evitar múltiples listeners en Astro View Transitions.
- Estandariza `VIEW_STATE_CHANGE` detail a `MAP_MAXIMIZED`.

### Detalle técnico — #351
- Reemplaza `[...set].find()` con iteración directa sobre `Set` en `CoordinateFinder.find()`.
- Evita allocación O(N) innecesaria. Medido ~40% más rápido en sets pequeños.

### Detalle técnico — #352
- Usa `logger` del proyecto en lugar de `console.error` en `FavoritesStore`.
- Integra `analytics.track('error_storage')` para monitoreo de fallos en producción.
- Añade tests de error en `src/tests/FavoritesStore.test.ts`.

---

## GRUPO 2 — ⚠️ CI verde en su rama pero `dirty` contra `main`

Requieren `git rebase main` antes de mergear. Ninguna tiene conflictos conceptuales insuperables.

| PR | Título | Archivos | +/- | Acción requerida |
|----|--------|----------|-----|-----------------|
| **#365** | Nexus Transfer Engine v3.3+ Architecture Hardening | 64 | +458/-4441 | Rebase + full CI |
| **#360** | Harden CI pipeline, remove dev artifacts | 48 | +538/-3684 | Rebase + suite completa |
| **#362** | SEO Optimizations + automated stats | 12 | +246/-97 | Rebase + verificar `sharp` dep |
| **#357** | Fix bug in spatial search cache | 7 | +37/-8 | Rebase simple |
| **#355** | Type safety for Popover API | 3 | +52/-81 | Rebase simple |
| **#363** | Docs: Auditoría técnica multiagente | 13 | +157/-358 | Rebase simple (solo docs) |
| **#341** | docs(adr): ADR-2026-002 | 13 | +149/-397 | Rebase simple (solo docs) |
| **#343** | feat: Astro Lit integration | 13 | +87/-359 | Esperar decisión sobre #342 |

### Notas por PR

**#365 — Prioridad alta**:
- Elimina todos los `any` types en la interfaz TypeScript↔WASM.
- Crea `RouteCalculatorModule` interface.
- Migra `public/sw.js` → `src/sw.ts` con tipado completo.
- Migra `public/js/*.js` → `src/scripts/*.ts` para bundling.
- Consolida tipos `Journey`/`RouteLeg`.
- Validación local reportada: 97/97 tests, `pnpm tsc --noEmit: 0 errors`, `cargo test: 15/15`.

**#360 — Hardening CI**:
- Elimina `patch_rutas.cjs` y `patch_astro_config.cjs` (scripts de agentes, no funcionales a largo plazo).
- Endurece `.gitignore` contra debris de sesiones de agentes.
- Corrige `telemetry.test.ts` con `vi.stubGlobal` en lugar de mutación directa de globals.
- Elimina `spatial-index` de `scripts/build-wasm.mjs` (no existe glue JS todavía).
- ⚠️ CI en esta PR solo corrió el check `copilot`. Necesita suite completa post-rebase.

**#362 — SEO**:
- Agrega `sharp` como nueva dependencia (OG image generation). Verificar que no rompe build en CI.
- Agrega `google-site-verification` y `msvalidate.01` via env vars.
- Mejora `sitemap.xml.ts` con rutas dinámicas.

**#357 — spatial-index cache**:
- Corrige hash de caché en `rust-wasm/spatial-index/src/lib.rs` para incluir coordenadas.
- Evita colisiones de caché entre datasets con los mismos IDs pero distintas coordenadas.

---

## GRUPO 3 — 🔴 Bloqueadas o acción especial

| PR | Problema | Decisión |
|----|----------|----------|
| **#342** | Elimina Tailwind CSS que aún está activo en producción | **BLOQUEAR** — no mergear hasta coordinar migración gradual |
| **#338** | Base branch es `speedy/inline-svg-icons-*`, no `main` | **CERRAR** y re-abrir contra `main` |
| **#333** | geo_transfer logic de marzo 10 — posiblemente ya en main vía #365 | **VERIFICAR** si los cambios están en main; si sí, **CERRAR** |

### Detalle — #342 bloqueo crítico
La PR elimina Tailwind CSS y sus plugins. Sin embargo:
- `README.md` en main documenta que Tailwind "sigue activo, siendo reemplazado progresivamente".
- La PR #360 corrigió explícitamente afirmaciones falsas de que Tailwind ya fue removido.
- Mergear #342 en este estado causaría ruptura de estilos en producción.
- **Ruta correcta**: crear un plan de migración incremental Tailwind→PostCSS/Houdini, documentado en un ADR, con etapas por componente.

---

## GRUPO 4 — 📝 Draft (no tocar)

| PR | Título | Nota |
|----|--------|------|
| **#369** | ⚡️ MueveCancun v3.3: UI Hardening & Data Sync | Draft |
| **#368** | feat(seo): OG image, verification tags | Draft |
| **#367** | feat(seo): professional OG + sitemap | Draft |
| **#364** | fix(wasm): build syntax v3.3.1 | Draft |
| **#335** | fix: duplicate imports, map init, silent fund loss | Draft |

---

## Orden de Merge Recomendado

```
Fase 1 — Inmediato (sin rebase):
  1. #366  ← seguridad, CVEs activos
  2. #350  ← fix funcional, sin riesgo
  3. #351  ← micro-perf, sin riesgo
  4. #352  ← observabilidad, sin riesgo

Fase 2 — Tras rebase sobre main actualizado:
  5. #355  ← type-safety limpio
  6. #357  ← bug fix en Rust (spatial-index)
  7. #341  ← docs ADR (solo archivos .md)
  8. #363  ← docs auditoría (solo archivos .md)
  9. #360  ← CI hardening (verificar suite completa)
 10. #365  ← TypeScript/WASM FFI hardening (mayor impacto)

Fase 3 — Con decisión de roadmap:
 11. #362  ← SEO (verificar sharp dep)
 12. #343  ← Lit integration (después de definir estrategia Tailwind)

Fase 4 — Acción manual:
 13. #338  ← cerrar y re-abrir contra main
 14. #342  ← bloquear hasta plan de migración
 15. #333  ← verificar si es duplicado, cerrar si ya está en main
```

---

## Métricas del Análisis

| Métrica | Valor |
|---------|-------|
| Total PRs analizadas | 20 |
| PRs listas para merge | 4 |
| PRs que necesitan rebase | 8 |
| PRs bloqueadas / acción especial | 3 |
| PRs draft (ignorar) | 5 |
| CVEs activos en deps | 3 (brace-expansion, picomatch ×2) |
| PRs con CI completo verde | 14 |
| PRs sin CI o CI parcial | 6 |

---

*Documento generado automáticamente por GitHub Copilot Agent el 2026-03-28.*
*Próxima auditoría recomendada: tras merge de Fase 2.*
