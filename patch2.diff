<<<<<<< SEARCH
      // Toggle Results List
      const toggleBtn = document.getElementById('toggle-list-btn');
      if (toggleBtn) {
          toggleBtn.addEventListener('click', () => {
              const resultsContainer = document.getElementById('results-container');
              const bestResultArea = document.getElementById('best-result-area');
              const toggleText = document.getElementById('toggle-list-text');
              const toggleIcon = document.getElementById('toggle-list-icon');
              const isHidden = resultsContainer?.classList.contains('hidden');

              if (isHidden) {
                  resultsContainer?.classList.remove('hidden');
                  bestResultArea?.classList.remove('hidden');
                  if (toggleText) toggleText.textContent = ui['calc.toggle.hide'] || 'Hide';
                  if (toggleIcon) toggleIcon.style.transform = '';
              } else {
                  resultsContainer?.classList.add('hidden');
                  bestResultArea?.classList.add('hidden');
                  if (toggleText) toggleText.textContent = ui['calc.toggle.show'] || 'Show';
                  if (toggleIcon) toggleIcon.style.transform = 'rotate(180deg)';
              }
          });
      }
=======
>>>>>>> REPLACE
