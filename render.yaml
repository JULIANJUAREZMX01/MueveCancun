services:
  - type: web
    name: cancunmueve-pwa
    runtime: node
    buildCommand: pnpm run build
    # Using npx serve as fallback for Web Service mode with static output
    startCommand: npx serve -s dist
    staticPublishPath: ./dist
    envVars:
      - key: NODE_VERSION
        value: 20.10.0
