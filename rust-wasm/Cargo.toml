[workspace]
members = [
    "route-calculator",
    "shared-types",
    "spatial-index",
    "eta-engine",
    "carbon-calculator",
]
resolver = "2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"

[profile.dev]
opt-level = 1  # Faster dev builds
