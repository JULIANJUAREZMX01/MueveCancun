import React from 'react';

const Map: React.FC = () => {
  return (
    <div className="w-full h-[400px] bg-slate-200 rounded-lg flex items-center justify-center">
      <p className="text-slate-500">Mapa de CancúnMueve (Mapbox)</p>
    </div>
  );
};

export default Map;
