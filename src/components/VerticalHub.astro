<template>
  <div class="vertical-hub">
    <JourneyCard v-for="journey in journeys" :key="journey.id" :journey="journey" />
    <GapAnalysisAlert v-if="showAlert" />
  </div>
</template>

<script setup>
import { ref } from 'vue';
import JourneyCard from './JourneyCard.vue';
import GapAnalysisAlert from './GapAnalysisAlert.vue';

const journeys = ref([]);
const showAlert = ref(false);

// Fetch journeys and gap analysis data
// Implement logic for gap analysis alert
</script>

<style scoped>
.vertical-hub {
  display: flex;
  flex-direction: column;
  gap: 20px;
}
</style>