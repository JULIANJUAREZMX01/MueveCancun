---
interface Props {
  class?: string;
  id?: string;
}

const { class: className, id } = Astro.props;
---

<div id={id} class={`bg-white rounded-2xl shadow-sm border border-slate-100 p-4 ${className || ''}`}>
  <slot />
</div>
