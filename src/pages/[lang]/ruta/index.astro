---
import { getRelativeLocaleUrl } from '../../../utils/utils';

export function getStaticPaths() {
  return [
    { params: { lang: "es" } },
    { params: { lang: "en" } },
  ];
}

const { lang } = Astro.params;
return Astro.redirect(getRelativeLocaleUrl(lang, 'rutas'));
---
