---
import MainLayout from '../../layouts/MainLayout.astro';
import fs from 'node:fs';
import path from 'node:path';
import init, { get_all_routes } from '../../../public/wasm/route-calculator/route_calculator.js';

// Load Coordinates for Statistics
import coordinates from '../../../public/coordinates.json';

let allRoutes = [];
let error = null;
let totalStops = 0;

try {
  // Load WASM on the server
  // process.cwd() is the project root in Node
  const wasmPath = path.join(process.cwd(), 'public/wasm/route-calculator/route_calculator_bg.wasm');
  const wasmBuffer = fs.readFileSync(wasmPath);

  await init(wasmBuffer);
  allRoutes = get_all_routes();
  
  // Sort routes by name for consistency
  allRoutes.sort((a, b) => a.name.localeCompare(b.name));

  // Calculate stats
  totalStops = Object.keys(coordinates).length;

} catch (e) {
  console.error("Server-side WASM load failed:", e);
  error = "No se pudo cargar el catálogo de rutas. Por favor intente más tarde.";
}

const TRANSPORT_LABELS = {
  "Bus": "Autobús",
  "Combi": "Combi",
  "Van": "Van / Colectivo",
  "ADO": "ADO",
  "PlayaExpress": "Playa Express",
  // Legacy support
  "Bus_Urban": "Autobús Urbano",
  "Bus_HotelZone": "Autobús Zona Hotelera",
  "Combi_Municipal": "Combi Municipal",
  "ADO_Airport": "ADO Aeropuerto",
  "Playa_Express": "Playa Express"
};

function getTransportLabel(type) {
  return TRANSPORT_LABELS[type] || type;
}
---

<MainLayout title="Catálogo de Rutas">
  <div class="space-y-6 animate-fade-in pb-10">
    <!-- Header -->
    <div class="pt-6 pb-2">
      <a href="/" class="inline-flex items-center text-sm font-bold text-slate-500 hover:text-primary-600 mb-4 transition-colors">
        ← Volver al Inicio
      </a>
      <h1 class="text-2xl font-display font-black text-deep-navy tracking-tight mb-1">
        Catálogo de Rutas
      </h1>
      <p class="text-slate-500 font-medium text-sm">
        Todas las opciones de transporte en Cancún
      </p>

      <!-- Stats Badge -->
      <div class="flex gap-3 mt-4">
        <div class="px-3 py-1 bg-white border border-slate-200 rounded-full shadow-sm flex items-center gap-2">
            <span class="text-xs font-bold text-slate-400 uppercase tracking-wider">Rutas</span>
            <span class="text-sm font-black text-slate-800">{allRoutes.length}</span>
        </div>
        <div class="px-3 py-1 bg-white border border-slate-200 rounded-full shadow-sm flex items-center gap-2">
            <span class="text-xs font-bold text-slate-400 uppercase tracking-wider">Paraderos</span>
            <span class="text-sm font-black text-slate-800">{totalStops}</span>
        </div>
      </div>
    </div>

    <!-- Routes List -->
    <div id="routes-container" class="space-y-4">
      {error && (
        <div class="text-red-500 font-bold p-4 bg-red-50 rounded-lg border border-red-100">
          {error}
        </div>
      )}

      {allRoutes.map(route => {
        let stopsPreview = `${route.origin_hub} ➔ ${route.dest_hub}`;
        if (route.stops && route.stops.length > 0) {
           stopsPreview = `${route.stops[0]} ➔ ${route.stops[route.stops.length - 1]}`;
        }

        return (
          <div class="p-4 bg-white rounded-lg shadow-sm border border-slate-100 active:scale-98 transition-transform mb-3">
            <div class="flex justify-between items-start">
              <div class="flex-1">
                <span class="inline-block px-2 py-0.5 rounded text-xs font-medium bg-blue-100 text-blue-800 mb-1">
                  {getTransportLabel(route.transport_type)}
                </span>
                <h3 class="font-bold text-slate-800">{route.name}</h3>

                <!-- Meta Info Row -->
                <div class="mt-2 flex flex-wrap gap-x-4 gap-y-2 text-xs text-slate-600">
                    {route.operator && (
                    <div class="flex items-center gap-1">
                        <span>🚌</span> <span class="font-medium">{route.operator}</span>
                    </div>
                    )}
                    {route.frequency && (
                     <div class="flex items-center gap-1">
                        <span>⏱️</span> <span>{route.frequency}</span>
                    </div>
                    )}
                    {route.schedule && (
                     <div class="flex items-center gap-1">
                        <span>🕒</span> <span>{route.schedule}</span>
                    </div>
                    )}
                </div>

                <div class="flex gap-2 mt-2 flex-wrap">
                   {route.badges.map(badge => (
                     <span class="text-xs bg-slate-100 px-1 rounded">{badge}</span>
                   ))}
                </div>
              </div>
              <div class="text-right ml-2 shrink-0">
                <span class="inline-flex items-center px-2 py-1 rounded bg-emerald-100 text-emerald-800 text-xs font-bold font-mono whitespace-nowrap">
                    💰 ${route.price.toFixed(2)}
                </span>
                <p class="text-xs text-slate-500 mt-1">{route.duration}</p>
              </div>
            </div>

            <!-- Stops Preview -->
            <div class="mt-3 pt-3 border-t border-slate-50 text-xs text-slate-500 flex items-center gap-1">
                <span>📍</span> <span class="truncate">{stopsPreview}</span>
            </div>
          </div>
        );
      })}
    </div>

  </div>
</MainLayout>
